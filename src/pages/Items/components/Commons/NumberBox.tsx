import { Box, styled } from "@mui/material";

export const NumberBoxStyle = styled(Box)(({}) => ({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  width: "100%",

  cursor: "pointer",
}));
